#include<stdio.h>
#include<conio.h>

int main()

{
   char ch1='\0',ch2='\0';

   printf("\n Enter Starting character:");
   ch1 = getche();

   printf("\n Enter Last character:");
   ch2 = getche();

   if(ch1<=ch2)
   {
     while(ch1<ch2)
     { 
       printf("\n %c",ch1++);
     }
   }
 
   else
      {
        while(ch1>=ch2)
        {
          printf("\n %c",ch1--);
        }
      }
    
       printf("\n Thanks!!!");
       getch();
       return 0;
}